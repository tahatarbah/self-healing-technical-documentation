# Getting started

Install and call the Sample Widget API.

```bash
npm install
```

Then import from the package entry:

```ts
import { createWidget } from "sample-widget-api";

const w = createWidget({ name: "demo" });
```

Continue with the [API reference](./api.md).

Orphan-ish link target that 404s: [Contributing](./contributing.md).
